import React, { FC } from "react";
import Layout from "../_components/(shared)/Layout";

const page: FC = () => {
  return (
    <Layout>
      <div>Stores Page</div>
    </Layout>
  );
};

export default page;
