import Layout from "@/app/_components/(shared)/Layout";
import React, { FC } from "react";
const page: FC = () => {
  return (
    <Layout>
      <div>Store Detail Page</div>
    </Layout>
  );
};

export default page;
