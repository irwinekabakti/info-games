import React, { FC } from "react";
import Layout from "../_components/(shared)/Layout";
const page: FC = () => {
  return (
    <Layout>
      <div>Creators Page</div>
    </Layout>
  );
};

export default page;
