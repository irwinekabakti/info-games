import Layout from "@/app/_components/(shared)/Layout";
import React, { FC } from "react";

const page: FC = () => {
  return (
    <Layout>
      <div>Game Detail Page</div>
    </Layout>
  );
};

export default page;
